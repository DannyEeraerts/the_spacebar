{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href ="{{ asset('css/login.css') }}">
    <link rel="stylesheet" href ="{{ asset('css/form.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row d-flex justify-content-center mt-4 ">
            <div class="col-12 col-lg-8 position px-0">
                <div class="border border-secondary">
                    {{ form_start(registrationForm,
                        {attr: {'novalidate': 'novalidate'}})
                    }}
                        <h1 class="h3 mb-3 text-center font-weight-normal">Register</h1>
                        {{ form_widget(registrationForm) }}
                        <button class="btn btn-lg btn-dark btn-block" type="submit">
                            Register
                        </button>
                    {{ form_end(registrationForm) }}

                    {#<form method="post" novalidate>
                        <div class="form-group">
                            <label for="inputFirstName" class="sr-only">FirstName</label>
                            <input type="text" name="firstName" id="inputFirstName"
                                   class="form-control" placeholder="Firstname" required autofocus>
                        </div>
                        <div class="form-group">
                            <label for="inputLastName" class="sr-only">LastName</label>
                            <input type="text" name="lastName" id="inputLastName"
                                   class="form-control" placeholder="Lastname" required autofocus>
                        </div>
                        <div class="form-group mb-0">
                            <label for="inputEmail" class="sr-only">Email</label>
                            <input type="email" name="email" id="inputEmail"
                                   class="form-control" placeholder="Email" required autofocus>
                            <small class="text-light">We never share your email</small>
                        </div>
                        <div class="input-group" id="show_hide_password">
                            <label for="inputPassword" class="sr-only">Password</label>
                            <input type="password" name="password" id="inputPassword"
                                   class= "form-control" placeholder="Password" required>
                            <div class="input-group-append">
                                <button class="btn btn-light border-none"><i class="password-eye fa fa-eye-slash"></i></button>
                            </div>
                        </div>
                        <div class="checkbox mb-3">
                            <label>
                                <input type="checkbox" name="_remember_me"> <small>Agree to terms I for sure reed</small>
                            </label>
                        </div>

                        <div >
                            <button class="btn btn-lg btn-dark btn-block" type="submit">
                                Register
                            </button>
                        </div>
                    </form>#}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/password_show.js') }}"></script>
    <script src="{{ asset('js/hide_error_message.js') }}"></script>
{% endblock %}
